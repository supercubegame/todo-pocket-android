# 口袋待办 v1.2 开发中

**尚无可交付 v1.2 APK。** 当前为原生待办、分类、活动路径、当天打卡及多日期账本的开发切片，不是完整产品。[已发布 v1.1](https://github.com/supercubegame/todo-pocket-android/releases/tag/preview-35575804413-1) · [v1.2 草稿 PR](https://github.com/supercubegame/todo-pocket-android/pull/4)。main、PR3、旧发布与手机数据未动。

## 原生界面与已知限制

TodayScreen/ActivitiesScreen/CalendarScreen 支持普通待办新增、编辑、勾选、筛选；分类新增、改名和上下按钮排序；分类内手动活动与有序路径；中国时区当天完成/跳过。日历账本多选日期仅汇总，不批量改账；每笔明确日期、类型、金额、备注，支出/退款/收入/预计分别显示，净支出与净现金按整数分计算。空选择为零，未记录日不等于确认零支出。

上个已验证源码 cdb23779，[运行35664758786](https://github.com/supercubegame/todo-pocket-android/actions/runs/35664758786) 两档各通过43项原生检查及158项数据库检查。真实点击验证15+20+45=80、退款5后净支出75、预计100独立、收入2.50后净现金-72.50及强停重开。当前新提交是否通过须看对应报告，不能借用上一提交结果。

SQLite 走单一后台执行器，独立 pocket-v12.db 不读取旧包或旧偏好。原生测试使用真实 adb 输入、节点断言、进程重启和独立 Android SQLite/WAL 回读，不给 UI DB 直接塞夹具。真实截图按源码/run/API发布并逐字节读回，但像素未目视验收；05-selected-dates 是应用选择后的账本，不是打开中的日历。选择状态临时保存，重进要重选；跨月/31日/闰月/大字体/生命周期/硬件返回尚待专项测试，未来实际日期目前允许输入。分类不是拖拽排序。

## 本轮：恢复预览安全接口（不是恢复界面）

新增 AppDatabase.prepareRestore 与 confirmRestore：完整验证 ZIP、schema2 状态、关联和媒体后，冻结私有暂存快照；返回不可改的当前/待恢复18表数量。预览不替换本机数据，不发布媒体。调用方必须展示替换影响、获得明确同意，再确认；取消或放弃页面必须 close 计划。

计划绑定数据库 helper 实例及会话，只能尝试确认一次；取消、失败、成功后重试都要重新预览。helper close 后失效，但未自动清理遗弃计划，生命周期/崩溃清理待 UI 接入。源文件在预览后被替换不会偷换已审核内容；确认读取冻结副本。确认还会重新校验暂存媒体摘要，损坏即拒绝。

过期保护比对完整规范状态（含修订号），不只比一个版本数字。提前检查用于减少无用媒体复制，最终比对必须与全部记录替换在同一个写事务，防止检查与写入之间丢更新。普通写入、另一个连接不更新修订号但改内容，也应拒绝旧预览；真正幂等无变化重试不应误判过期。完整媒体复制与暂存清理在事务提交前完成。

设备测试新增预览无副作用、独立数量、不可改摘要、取消、来源绑定、过期保留、同修订号内容变化、暂存媒体损坏、准确后置故障回滚、源文件替换、成功/失败重放拒绝、幂等无变化及独立进程重开。报告读取完整阶段输出并逐项核对必需检查，不依赖截断日志尾；解析器另有正负自测。索引 reports/restore-plan-<SHA>-<run>.json 明确标识 GUARDED_RESTORE_BACKEND_NOT_SAF_UI，回写后末尾作业断言拒绝缺失覆盖。

**这不是已接通的文件选择、确认弹窗或撤销恢复。** 原生界面仍不开放恢复按钮。底层旧 restoreBackup 仍保留并绕过预览，未来用户界面只可用新受保护路径；后台接口本身不能证明用户点过同意。并发压力、最终两次检查之间的确定性竞争注入及关页/进程中断清理未专项验证。

## 数据、备份和媒体边界

原生 Java、SQLiteOpenHelper schema2、FK/WAL/事务。活动、应用、路径/标签、分精度账本及重试/撤销日志、打卡日期与录入时间、有序私有/公开图文块、媒体登记、八类字段及字段笔记、独立普通待办。稳定身份，字段归档保留历史并拒绝新值/新关联。新数据不继承旧500条/200字配额，但资源有限，不承诺物理无限；列表与汇总全量加载。

旧Todo备份单独验证后事务追加，以来源摘要和映射ID防同文件重复，改变内容的备份可能追加语义相似项；不会发明活动/打卡/钱。导入 UI/撤销未做。schema1到2仅新增表迁移，冻结旧DDL夹具只覆盖部分旧数据，并非所有升级异常。旧 TodoModel/BackupCodec/CoreTest 不动。

全备份固定18表/列/类型、严格UTF8/长度/尾随校验，未知表拒绝以免漏备份；可信DDL临时库验证外键、字段（含归档值）、日期、顺序和账本日志，不执行备份SQL。快照保留ID、排序、私有内容、修订和批次/导入日志。完整备份包含私有内容且未加密，不能当分享包；摘要不是来源认证。

媒体集合/大小/摘要必须匹配；不可变内容ID私有复制用锁和原子移动，不删除原相册。恢复是替换，不是合并；失败保留旧DB/媒体，但可能留下新孤立字节（含确认时发现暂存损坏的情况），不可盲删，安全GC待做。暂存清理先于提交，数据库内规范读回。8MiB状态/清单预算是暂定内存保护，不是容量实测。断电、磁盘写满、跨进程压力、真实照片解码、SAF/云盘均非已验收能力。

旧设备夹具保持18表非空、独立编码器、真实ZIP和合成媒体、合法ZIP中的语义损坏拒绝、精确触发器后置故障及整库回滚、重复恢复、重启后日志可用。原158项保留，新预览检查是增量；合成字节不等于真实图片。

## 验证和下一步

快闸门 `python3 tools/verify.py v12` 使用真实JDK17，旧核心54、领域82、文件49。Android执行 `gradle --no-daemon --console=plain assembleDebug assembleDebugAndroidTest lintDebug`，隔离CI分别 `TEST_API=26 python3 tools/emulator_gate.py` 和34；验证包/签名/安装、独立seed/reopen、原生UI43。每次按实际报告计数，不把双系统同套检查当成更多独立功能。

报告区分 schema2 DB/media、受保护恢复后台、原生切片和完整产品，release_ready=false；顶部JVM scope只描述快闸门。完整报告 reports/<SHA>-<run>.json，原生/截图索引 reports/native-<SHA>-<run>.json，新恢复索引独立。先行 f51cc556 用反射要求新接口，允许编译后实际设备暴露缺失；没有通过之前不声称实现验证完成。

仍待：SAF恢复预览确认和撤销、真实图文笔记与图片选择/解码/缩略图/裁剪遮挡、PDF/分段图片/Markdown资源包分享、应用目录/字段UI、分类拖拽、待办删除排序、打卡历史/补记/未记录、计划/搜索/模板/归档、生命周期/大字体/规模/视觉与真机验收。分享私有过滤不是图像遮挡，不能附带未遮挡原图。

内部包 com.supercubegame.pockettodo.v12.preview、1.2/code3、min26/target34/compile35、desugar_jdk_libs2.1.5。`python3 tools/verify.py build` 继续阻断完整产品交付，其历史错误文本与负向断言耦合，不等于内部APK没有编译。无APK上传/Release/付费服务；稳定签名另行可信本地生成与备份，密钥不进聊天/源码/日志。批准路径：[docs/V1_2_PLAN.md](docs/V1_2_PLAN.md)。
